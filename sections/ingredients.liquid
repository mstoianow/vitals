<div class="section color-{{ section.settings.color_scheme }}" style="--section-padding-block: {{ section.settings.padding_block }}px;">
  <div class="page-width">
    <div class="ingredients">
      {%- if section.settings.image != blank -%}
        <div class="ingredients__media media media--portrait">
          {{
            section.settings.image
            | image_url: width: 1200
            | image_tag: loading: 'lazy', sizes: '(min-width: 990px) 45vw, 100vw', widths: '400, 600, 900, 1200', alt: section.settings.image.alt
          }}
        </div>
      {%- endif -%}

      <div class="ingredients__content">
        <div class="ingredients__header">
          {%- if section.settings.eyebrow != blank -%}
            <span class="eyebrow">{{ section.settings.eyebrow }}</span>
          {%- endif -%}
          {%- if section.settings.heading != blank -%}
            <h2 class="h2">{{ section.settings.heading }}</h2>
          {%- endif -%}
          {%- if section.settings.text != blank -%}
            <div class="rte text-muted">{{ section.settings.text }}</div>
          {%- endif -%}
        </div>

        <ul class="ingredients__list">
          {%- for block in section.blocks -%}
            <li class="ingredients__item" {{ block.shopify_attributes }}>
              <div class="ingredients__row">
                <h3 class="ingredients__name">{{ block.settings.name }}</h3>
                <span class="ingredients__amount">{{ block.settings.amount }}</span>
              </div>
              {%- if block.settings.description != blank -%}
                <p class="ingredients__description text-small text-muted">{{ block.settings.description }}</p>
              {%- endif -%}
            </li>
          {%- endfor -%}
        </ul>

        {%- if section.settings.footnote != blank -%}
          <p class="ingredients__footnote text-small text-muted">{{ section.settings.footnote }}</p>
        {%- endif -%}
      </div>
    </div>
  </div>
</div>

{% stylesheet %}
  .ingredients {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
    align-items: start;
  }

  .ingredients__content {
    display: flex;
    flex-direction: column;
    gap: 1.75rem;
  }

  .ingredients__header {
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
  }

  .ingredients__list {
    display: flex;
    flex-direction: column;
  }

  .ingredients__item {
    padding-block: 1rem;
    border-bottom: 1px solid rgb(var(--color-border));
  }

  .ingredients__item:first-child {
    border-top: 1px solid rgb(var(--color-border));
  }

  .ingredients__row {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
  }

  .ingredients__name {
    margin: 0;
    font-family: var(--font-heading-family);
    font-size: 1.3rem;
    font-weight: var(--font-heading-weight);
    letter-spacing: 0;
    text-transform: none;
  }

  .ingredients__amount {
    font-size: 0.85rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    white-space: nowrap;
  }

  .ingredients__description {
    margin: 0.35rem 0 0;
  }

  .ingredients__footnote {
    margin: 0;
  }

  @media screen and (min-width: 990px) {
    .ingredients {
      grid-template-columns: minmax(0, 5fr) minmax(0, 6fr);
      gap: 4rem;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Ingredients",
  "tag": "section",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Colour scheme",
      "default": "scheme-1"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Какво съдържа"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "Heading",
      "default": "Всяко активно вещество, всяка доза"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Text",
      "default": "<p>Без патентовани смеси. Ето какво точно съдържа всяка дневна доза и защо е там.</p>"
    },
    {
      "type": "text",
      "id": "footnote",
      "label": "Footnote",
      "default": "Количествата са посочени за дневна доза от две капсули."
    },
    {
      "type": "range",
      "id": "padding_block",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Vertical padding",
      "default": 64
    }
  ],
  "blocks": [
    {
      "type": "ingredient",
      "name": "Ingredient",
      "settings": [
        {
          "type": "text",
          "id": "name",
          "label": "Name",
          "default": "Витамин D3 (холекалциферол)"
        },
        {
          "type": "text",
          "id": "amount",
          "label": "Amount per serving",
          "default": "4000 МЕ"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Description",
          "default": "Допринася за нормалната функция на имунната система и за поддържането на нормални кости и зъби."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Ingredients",
      "blocks": [
        {
          "type": "ingredient",
          "settings": {
            "name": "Витамин D3 (холекалциферол)",
            "amount": "4000 МЕ",
            "description": "Допринася за нормалната функция на имунната система и за поддържането на нормални кости и зъби."
          }
        },
        {
          "type": "ingredient",
          "settings": {
            "name": "Магнезиев бисглицинат",
            "amount": "300 мг",
            "description": "Високоусвоима форма, която допринася за нормалната мускулна функция и за нормалната функция на нервната система."
          }
        },
        {
          "type": "ingredient",
          "settings": {
            "name": "Витамин K2 (MK-7)",
            "amount": "100 мкг",
            "description": "Действа заедно с витамин D и допринася за поддържането на нормални кости."
          }
        }
      ]
    }
  ]
}
{% endschema %}
